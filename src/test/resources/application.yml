spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create  # 자동으로 테이블을 만들어준다
#    properties:
#      hibernate:
#        # show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug  # logger를 통해 hibernate가 생성하는 sql이 보여진다.
    # org.hibernate.type: trace